control-construct:
  type: array
  x-key: element-name
  items:
    oneOf:
      - description: 'controller'
        type: object
        required:
          - element-name
          - category
          - _template
          - logical-termination-point
        properties:
          element-name:
            type: string
            description: >
              'controllerName
              candidate: from [DDM://v1/regard-controller$request.body#controller-name]
              running: from candidate
              operational: from [CDM://v1/provide-list-of-controllers/controller-name], delete entire CC from list if not in response'
          category:
            type: string
            enum:
              - 'controller'
          _template:
            type: string
            description: >
              'controllerTemplateName (must match the template-name of a ControllerProfile definition)
              candidate: from [DDM://v1/regard-controller$request.body#controller-template-name]
              running: from candidate
              operational: ???we could add a service to CDM for reading this information???'
          logical-termination-point:
            type: array
            x-key: local-id
            items:
              oneOf:
                - description: 'LogicalTerminationPoint - see 12_MountPoint.yaml'
                  type: object
